apiVersion: apps/v1
kind: Deployment
metadata:
  name: micro-test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: micro-test
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: micro-test
    spec:
      containers:
      - name: micro-test
        image: jepullaguari/micro_pagos
        imagePullPolicy: Always
